sudo apt install zsh fonts-powerline curl
chsh -s $(which zsh)
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
echo "reboot for fonts and install to take affect"
